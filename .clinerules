# jintao_node_eye · Roo Code 行为规则
#
# 目标：最小化 Token 消耗，最大化精准度
# 注意：本文件等效于 Cursor 的 .cursorrules，仅 Roo Code 读取

---

## 一、首次进入项目

1. **先读 `AI_INDEX.md`** — 一次性理解全项目结构、依赖关系、入口函数（~200 tokens）。
2. **不递归遍历目录** — 读完 `AI_INDEX.md` 直接定位到目标文件。
3. **不执行 `ls`、`grep`、`find`、`list_files` 等探索性命令**，除非 `AI_INDEX.md` 中找不到所需信息。

---

## 二、文件读取规则

1. **只读目标文件** — 用户指定哪个文件就读哪个，不扫描无关目录。
2. **精确行号** — 优先使用 `AI_INDEX.md` 第六节给出的行号范围，只读目标函数附近的代码块（~50-80 行）。
3. **不读大文件** — 超过 200 行的文件必须用 offset/limit 分段读取。
4. **不读二进制文件** — `.png`、`.jpg`、`.blend`、`.mp4`、`.mkv`、`.mp3` 等非文本文件直接跳过。

---

## 三、修改代码规则

1. **最小改动** — 只改目标函数/类，不改无关代码。
2. **Diff 优先** — 修改时用 apply_diff 工具，不上传全量文件。
3. **一次只改一件事** — 不要在一次交互中混合多个不相关的修改。

---

## 四、对话管理

1. **单任务单次对话** — 一个独立任务完成后，建议用户点击 New Task 开启新对话。
2. **不在一次对话中做多个无关任务** — 防止历史 Token 膨胀。
3. **不需要的文件不要读** — 用户没提到的文件绝不主动读取。

---

## 五、禁止行为

| 禁止动作 | 原因 | 替代方案 |
|---------|------|---------|
| 递归遍历目录 | 消耗海量 Token 且无意义 | 读 `AI_INDEX.md` |
| 读取不需要的文件 | 浪费上下文窗口 | 只读用户 @ 指定的文件 |
| 在对话中输出全量文件内容 | Token 开销大 | 用 diff 或指明行号 |
| 读取图片/视频/二进制文件 | 无法从中获取代码信息 | 直接跳过 |
| 修改无关模块 | 引入 bug 风险 | 聚焦目标模块 |

---

## 六、用户专属工作流（5 阶段循环）

用户的工作流程是：**问问题 → 讨论定方案 → 写合同(.md) → 写代码 → 测试 → 发现差距 → 再讨论 → 再修改**

AI 必须按以下阶段规则响应：

### 阶段 1：❓ 问问题 & 讨论（Ask 模式）
- **目标**：回答用户问题，讨论方案，确定思路
- **规则**：
  - 只读 `AI_INDEX.md` 和用户提到的文件，不读无关代码
  - 讨论时**不修改任何代码文件**
  - 讨论结果用文字总结，不输出全量文件内容
  - 当方案确定后，**等待用户明确指示再切换模式**（不主动调用 switch_mode）

### 阶段 2：📝 写合同（Architect 模式）
- **目标**：将讨论确定的方案写成 `.md` 合同文件
- **规则**：
  - 只写 `合同/` 目录下的 `.md` 文件
  - 合同格式参考 `合同/合同规范.md`
  - 写完合同后，**等待用户明确指示再切换模式**（不主动调用 switch_mode）

### 阶段 3：💻 写代码（Code 模式）
- **目标**：根据合同实现代码
- **规则**：
  - 只改目标函数/类，不改无关代码
  - 修改用 diff 格式，不上传全量文件
  - 一次只改一件事

### 阶段 4：🔍 测试 & 发现问题
- **目标**：用户自行测试，发现与预期的差距
- **规则**：
  - 用户反馈问题后，先分析是「合同问题」还是「代码实现问题」
  - 如果是合同问题 → 建议切 Architect 模式修改合同
  - 如果是代码问题 → 建议切 Code 模式修改代码
  - 如果不确定 → 建议切 Ask 模式讨论

### 阶段 5：🔄 循环修正
- 每次修改只聚焦当前问题
- 修改完成后，建议用户开启 **New Task** 进入下一轮
- 不要在同一个对话中混合「讨论新功能」和「修改旧代码」

---

## 七、Token 节省专项规则

1. **每次响应前自问**：用户当前在哪个阶段？只做该阶段的事
2. **不主动提供额外信息** — 用户没问的绝不主动说
3. **讨论阶段不读代码文件** — 只读 `AI_INDEX.md` 和合同文件
4. **合同阶段不读代码文件** — 只读 `合同/` 目录
5. **代码阶段不读合同文件** — 只读目标代码文件
6. **每次对话结束时**，建议用户点击 `New Task` 开启新对话