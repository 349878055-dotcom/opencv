# ecursor S01 · 出厂链与工作流对照（定稿）

## 一、产品主链（文字定稿）

```text
前1 自然语言
    → 写入 01_滑杆包.json + 01_操作台上下文.json（NL / Prompt / 知识库 预留）

① 操作台（Comfy 节点 2，不再依赖网页）
    · 自然语言 / Prompt / 知识库 / 16 预设 / 六根宏滑杆 + 盯住段
    · 出厂：01_滑杆包 + 02_滑杆_L1纠正（Python finalize）
    · `tools/能量工作台.html` 仅备用，Queue 不打开浏览器

```

**L1 禁区**：挂在操作台里，不是 Comfy 上的「定稿」节点。

---

## 二、Comfy `ecursor_S01.json`（7 节点 · 6 条线）

| id | 菜单名 | 作用 |
|----|--------|------|
| 1 | 前1 自然语言→操作台 | 初稿滑杆 + 写上下文 |
| 2 | ① 操作台(Comfy滑杆+L1) | 人调；出厂 `02_滑杆_L1纠正.json` → **直连 ②** |
| 3 | ② 能量包络 | `03_能量包络.json` |
| 4 | ③ 全量12×150 | `04_全量_包络展开.json` |
| 5 | ④ 真人律 | `05_全量_真人律.json` |
| 6 | ④b 平庸纠正 | `06_全量_平庸纠正.json` |

改代码：`nodes_v1.py` 搜 `节点 ①` / `JintaoEye_OpenWorkbench`。

---

## 三、操作台附件文件

| 文件 | 内容 |
|------|------|
| `指令/01_操作台上下文.json` | natural_language, prompt, knowledge_base, l1_attachment |
| `指令/01_自然语言.txt` | 与 NL 字段同步 |
| `tools/01_操作台上下文.json` | HTTP 服务时的副本 |

API：`GET /workbench_context.json` · `POST /save_context` · `POST /save_packet`（含 L1 finalize）

---

## 四、你怎么跑

1. Reload 自定义节点，加载 `workflows/ecursor_S01.json`
2. `./scripts/s01_打开能量工作台.sh` → 浏览器改滑杆 / 附件 → **保存到资产库**

改烘焙逻辑：搜 `nodes_v1.py` 里 `_bake_from_dense_path`。
